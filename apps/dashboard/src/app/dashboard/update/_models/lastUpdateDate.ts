export const DEFAULT_LAST_UPDATE_DATE = {
  date: null,
  time: null,
  elapsedDays: null,
};

export type LastUpdateDate = {
  date: string | null;
  time: string | null;
  elapsedDays: string | null;
}
